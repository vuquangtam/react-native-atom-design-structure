import * as AppActions from './index.slice';
import * as AppSelectors from './index.selector';
import * as AppEpics from './index.epic';
import AppReducer from './index.slice';

export {
    AppActions,
    AppSelectors,
    AppEpics,
    AppReducer
}